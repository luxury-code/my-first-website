# 컬러 팔레트 디자인 시스템

## 프로젝트 정보

- **출처 웹사이트**: AI서치랩 — AI Search Optimization Lab (by MASSTIDE)
- **분석 날짜**: 2026-08-18
- **적용 프로젝트**: 포트폴리오 웹사이트
- **분석 방법**: 원본 스크린샷(`image.png`, 650×605px)의 전체 픽셀 히스토그램 + 영역별 샘플링
- **버전**: v1.0

> 이 문서의 모든 Hex 코드는 추정값이 아니라 원본 이미지에서 **직접 추출한 실측값**입니다.
> 단, `--color-primary-dark`, `--color-text-muted`, `--color-link-hover` 3개는
> 접근성(WCAG AA) 기준을 충족시키기 위해 브랜드 색조(Hue 72°)를 유지한 채 **파생시킨 값**이며,
> 아래 [부록 A](#부록-a-파생-컬러-산출-근거)에 계산 과정을 남겨두었습니다.

---

## CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary: #DDFF50;        /* 네온 라임 · 브랜드 시그니처 (원본 점유율 17.3%) */
  --color-primary-light: #ECFF9B;  /* 라임 카드 내 칩/태그 배경 */
  --color-primary-dark: #C3E046;   /* 버튼 hover·pressed 상태 */

  /* Secondary Colors */
  --color-secondary: #0B0B0B;      /* 소프트 블랙 · 구조 색상 (점유율 24.2%) */
  --color-accent: #CAB8F6;         /* 라벤더 퍼플 · 강조 카드 (점유율 8.6%) */

  /* Background Colors */
  --color-bg-primary: #FFFFFF;     /* 라이트 섹션 페이지 배경 */
  --color-bg-secondary: #0B0B0B;   /* 다크 섹션 배경 */

  /* Text Colors */
  --color-text-primary: #0B0B0B;   /* 본문 기본 (라이트 배경 위) */
  --color-text-secondary: #ADADAD; /* 2차 정보 (다크 배경 위) */
  --color-text-muted: #8A8A8A;     /* 캡션·보조 설명 (AA 보정값) */

  /* Interactive Colors */
  --color-button-primary: #DDFF50; /* CTA 버튼 배경 */
  --color-button-hover: #C3E046;   /* CTA 버튼 hover */
  --color-link: #0B0B0B;           /* 라이트 배경 위 링크 */
  --color-link-hover: #627A00;     /* 링크 hover (Lime Ink) */
}
```

### 확장 토큰

위 12개만으로는 다크 섹션과 카테고리 카드를 표현할 수 없어, 실제 구현에 필요한 토큰을 추가로 정의합니다.

```css
:root {
  /* Surface — 배경 위에 얹히는 면 */
  --color-surface-subtle: #F4F4F2;     /* 네비게이션 캡슐 (오프화이트) */
  --color-surface-dark: #191919;       /* 다크 섹션 내부 카드 */

  /* Accent 확장 — 카드 구획용 파스텔 */
  --color-accent-cyan: #7FE0F0;        /* STEP 03 카드 (점유율 3.7%) */
  --color-accent-mint: #7FE0C1;        /* 카테고리: GEO */
  --color-accent-blue: #A8C7FF;        /* 카테고리: SEO */
  --color-accent-peach: #FFCBA9;       /* 카테고리: AEO */

  /* Text 확장 */
  --color-text-inverse: #FFFFFF;       /* 다크 배경 위 본문 */
  --color-text-brand: #627A00;         /* 흰 배경 위에서 쓰는 유일한 라임 텍스트 */

  /* Border */
  --color-border-light: #F8F8F7;       /* 라이트 영역 미세 경계 */
  --color-border-dark: #171717;        /* 다크 섹션 구분선 */
  --color-border-dark-strong: #1F1F1F; /* 다크 카드 외곽선 */

  /* Chip — 각 파스텔 카드 위에 얹는 태그 배경 */
  --color-chip-on-lime: #ECFF9B;
  --color-chip-on-purple: #B0A2D6;
  --color-chip-on-cyan: #70C3D1;

  /* Radius / Shadow — 이 팔레트가 전제하는 형태 언어 */
  --radius-card: 24px;
  --radius-pill: 999px;
  --shadow-card: none;  /* 네오브루탈리즘: 그림자 대신 색면으로 층위 표현 */
}
```

---

## 컬러 사용 가이드

### 1. 색상별 역할과 사용법

#### `--color-primary` `#DDFF50` — 네온 라임

이 시스템에서 **가장 강한 신호**입니다. 채도 100%의 형광 계열이라 화면에 등장하는 순간 시선을 독점합니다.

**사용해야 할 곳**

- 페이지당 **주요 CTA 버튼 1개** (예: "프로젝트 문의하기")
- 히어로 섹션의 대형 색면 배경
- 브랜드 로고 포인트, 카테고리 뱃지
- 다크 배경 위 강조 텍스트 (`#0B0B0B` 위에서 17.33:1 — AAA 통과)

**사용하면 안 되는 곳**

- ❌ **흰 배경 위 텍스트 색** — 대비비 1.14:1로 사실상 읽을 수 없습니다. 흰 배경에서 라임 느낌이 필요하면 반드시 `--color-text-brand` `#627A00`을 쓰세요.
- ❌ 한 화면에 3개 이상의 라임 CTA — 강조가 분산되면 아무것도 강조되지 않습니다.
- ❌ 넓은 면적의 본문 배경 — 형광색은 장시간 응시 시 눈의 피로를 유발합니다. 히어로처럼 스크롤로 지나가는 영역에만 쓰세요.

```css
.btn-primary {
  background: var(--color-button-primary);
  color: var(--color-secondary);      /* 라임 위에는 항상 검정 */
  border: none;
  border-radius: var(--radius-pill);
}
.btn-primary:hover  { background: var(--color-button-hover); }
.btn-primary:active { background: var(--color-button-hover); transform: translateY(1px); }
```

#### `--color-secondary` `#0B0B0B` — 소프트 블랙

순수 검정(`#000000`)이 아닌 **살짝 들린 검정**입니다. 순검정은 화면에서 너무 딱딱하게 읽히기 때문에, 이 밝기 조정은 의도된 선택입니다. 포트폴리오에서도 `#000`으로 바꾸지 마세요.

- 라이트 섹션에서는 **텍스트 색**으로
- 다크 섹션에서는 **배경 색**으로
- 라임 카드 위 원형 아이콘 버튼 배경으로

#### `--color-accent` `#CAB8F6` 및 파스텔 계열

퍼플·시안·민트·블루·피치 5색은 모두 **명도 80~88% 대역에 정렬**되어 있습니다. 그래서 여러 색을 동시에 써도 산만해지지 않고 한 세트로 읽힙니다.

**핵심 규칙: 파스텔은 "장식"이 아니라 "정보 코드"입니다.**

| 파스텔 | Hex | 원본에서의 의미 | 포트폴리오 적용 예 |
|---|---|---|---|
| 라벤더 | `#CAB8F6` | STEP 02 | 프로젝트 카테고리 A |
| 시안 | `#7FE0F0` | STEP 03 | 프로젝트 카테고리 B |
| 민트 | `#7FE0C1` | GEO | 기술 스택 태그 |
| 블루 | `#A8C7FF` | SEO | 기술 스택 태그 |
| 피치 | `#FFCBA9` | AEO | 기술 스택 태그 |

같은 종류의 정보에는 **항상 같은 파스텔**을 쓰세요. 색이 의미를 잃으면 그냥 소음이 됩니다.

#### 텍스트 위계 4단계

| 토큰 | Hex | 배경 | 대비비 | 용도 |
|---|---|---|---:|---|
| `--color-text-primary` | `#0B0B0B` | `#FFFFFF` | 19.68:1 | 제목·본문 |
| `--color-text-inverse` | `#FFFFFF` | `#0B0B0B` | 19.68:1 | 다크 섹션 제목·수치 |
| `--color-text-secondary` | `#ADADAD` | `#0B0B0B` | 8.77:1 | 다크 위 2차 정보 |
| `--color-text-muted` | `#8A8A8A` | `#0B0B0B` | 5.70:1 | 캡션·메타 정보 |

> ⚠️ **원본 사이트의 함정**: 원본은 다크 배경 위 보조 텍스트에 `#747474`를 썼는데, 대비비가 **4.21:1**로 WCAG AA(4.5:1)에 미달합니다. 이 시스템에서는 `#8A8A8A`(5.70:1)로 올려 채택했습니다. 원본을 그대로 따라 하지 마세요.

### 2. 배경-텍스트 조합 매트릭스

안전한 조합만 정리했습니다. 표에 없는 조합은 쓰기 전에 대비비를 확인하세요.

| 배경 | 본문 텍스트 | 보조 텍스트 | 강조/링크 |
|---|---|---|---|
| `#FFFFFF` 흰색 | `#0B0B0B` ✅ 19.7:1 | `#5A5A5A` ✅ 7.0:1 | `#627A00` ✅ 4.9:1 |
| `#0B0B0B` 다크 | `#FFFFFF` ✅ 19.7:1 | `#8A8A8A` ✅ 5.7:1 | `#DDFF50` ✅ 17.3:1 |
| `#191919` 다크 카드 | `#FFFFFF` ✅ 17.6:1 | `#8A8A8A` ✅ 5.1:1 | `#DDFF50` ✅ 15.5:1 |
| `#DDFF50` 라임 | `#0B0B0B` ✅ 17.3:1 | `#0B0B0B` 60% 투명 | — |
| `#CAB8F6` 라벤더 | `#0B0B0B` ✅ 11.0:1 | `#0B0B0B` 65% 투명 | — |
| `#7FE0F0` 시안 | `#0B0B0B` ✅ 13.0:1 | `#0B0B0B` 65% 투명 | — |

**절대 금지 조합**

- ❌ `#DDFF50` 텍스트 on `#FFFFFF` (1.14:1)
- ❌ `#FFFFFF` 텍스트 on `#DDFF50` (1.14:1)
- ❌ `#FFFFFF` 텍스트 on 모든 파스텔 (1.1~1.8:1)
- ❌ 파스텔끼리의 조합 (명도가 같아 경계가 사라짐)

파스텔 위 보조 텍스트는 별도 회색을 만들지 말고 **검정에 투명도**를 주세요. 배경색이 비쳐 톤이 자동으로 맞습니다.

```css
.card-pastel .text-sub { color: rgb(11 11 11 / 0.65); }
```

### 3. 컬러 사용 비율 — 60/30/10 규칙

원본의 실측 점유율이 이 비율과 거의 정확히 일치합니다. 포트폴리오에서도 이 균형을 유지하세요.

| 비중 | 역할 | 색상 | 원본 실측 |
|---:|---|---|---:|
| **60%** | 중립 골격 | `#FFFFFF` + `#0B0B0B` + `#191919` | 52.5% |
| **30%** | 브랜드 색면 | `#DDFF50` | 17.3% |
| **10%** | 정보 구획 | 파스텔 5종 | 14.4% |

> 라임이 17%를 넘어가기 시작하면 "브랜드"가 아니라 "형광펜"처럼 보입니다. 한 화면을 캡처해서 라임 면적이 1/5을 넘으면 줄이세요.

### 4. 상태(State) 정의

```css
/* 버튼 — 라임 계열은 어둡게 눌러서 hover 표현 */
.btn-primary:hover    { background: #C3E046; }
.btn-primary:active   { background: #B0CC3E; }
.btn-primary:disabled {
  background: #E8EDD4;
  color: #9AA08A;
  cursor: not-allowed;
}

/* 버튼 — 다크 계열은 밝게 들어서 hover 표현 */
.btn-dark:hover  { background: #191919; }
.btn-dark:active { background: #1F1F1F; }

/* 링크 — 색만 바꾸지 말고 밑줄을 함께 쓸 것 */
a {
  color: var(--color-link);
  text-decoration: underline;
  text-underline-offset: 3px;
  text-decoration-color: rgb(11 11 11 / 0.3);
  transition: color .18s ease, text-decoration-color .18s ease;
}
a:hover {
  color: var(--color-link-hover);
  text-decoration-color: currentColor;
}

/* 포커스 — 키보드 사용자를 위해 반드시 유지 */
:focus-visible {
  outline: 3px solid var(--color-primary);
  outline-offset: 2px;
  border-radius: 4px;
}
/* 라임 배경 위에서는 라임 outline이 보이지 않으므로 반전 */
.bg-lime :focus-visible { outline-color: var(--color-secondary); }
```

> 💡 링크에 **색만으로** 상태를 표현하지 마세요. WCAG 1.4.1(색에 의존하지 않기) 위반이며, 색각 이상 사용자에게는 아무 변화도 없는 것과 같습니다. 밑줄을 항상 함께 쓰세요.

### 5. 형태 언어 — 색만 가져오면 안 되는 이유

이 팔레트는 **네오브루탈리즘(Neo-brutalism)** 문법 위에서 설계되었습니다. 다음 3가지를 함께 가져와야 원본의 인상이 재현됩니다.

1. **큰 라운드 코너** (`24px` 내외) — 강한 색면의 공격성을 완화
2. **그림자 없음** — 층위는 그림자가 아니라 **색면 대비**로 표현
3. **넓은 여백** — 색이 강한 만큼 숨 쉴 공간이 필요

```css
.card {
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);  /* none */
  padding: 32px;
}
```

색상만 복사하고 그림자를 얹으면 2010년대 머티리얼 디자인처럼 보입니다. 주의하세요.

---

## 반응형 고려사항

### 1. 다크모드 대응

이 팔레트는 원본부터 라이트/다크 섹션이 공존하는 구조라 다크모드 전환이 자연스럽습니다. 핵심은 **라임과 파스텔의 채도를 낮춰주는 것**입니다. 어두운 화면에서 형광색은 실제보다 더 밝게 튀어 보이기 때문입니다(퍼킨제 효과).

```css
/* 기본값(라이트)은 :root에 정의 — 위 "CSS 변수 정의" 참조 */

@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) {
    --color-bg-primary: #0B0B0B;
    --color-bg-secondary: #191919;

    --color-text-primary: #FFFFFF;
    --color-text-secondary: #ADADAD;
    --color-text-muted: #8A8A8A;

    /* 라임: 채도를 살짝 눌러 눈부심 완화 */
    --color-primary: #D0F53D;
    --color-primary-light: #E3FF70;
    --color-primary-dark: #B8DC38;

    /* 파스텔: 명도를 낮춰 다크 배경과의 눈부신 대비 완화 */
    --color-accent: #A594D4;
    --color-accent-cyan: #62BCCC;
    --color-accent-mint: #62BC9F;
    --color-accent-blue: #8AA6D9;
    --color-accent-peach: #D9A98A;

    --color-surface-subtle: #191919;
    --color-surface-dark: #1F1F1F;
    --color-border-light: #1F1F1F;

    /* 다크에서는 링크가 라임이 됨 */
    --color-link: #D0F53D;
    --color-link-hover: #E3FF70;
  }
}

/* 사용자가 수동 토글한 경우 — 미디어쿼리보다 우선 */
:root[data-theme="dark"] {
  /* 위 dark 블록과 동일한 값을 반복 정의 */
}
```

**다크모드 3원칙**

- 모든 색은 **`:root`(라이트)에서 먼저 정의**하고, 다크에서는 값만 덮어씁니다. 미디어쿼리 안에서만 정의된 색이 있으면 라이트 모드에서 그 색이 사라집니다.
- `body`에 `background: var(--color-bg-primary)`를 **명시적으로** 지정하세요. 투명한 body는 브라우저/호스트 배경을 그대로 노출합니다.
- 다크에서 파스텔을 그대로 쓰면 눈이 아픕니다. 명도를 15~20%p 낮추세요.

### 2. 화면 크기별 컬러 면적 조정

색 자체는 브레이크포인트마다 바뀌지 않지만, **차지하는 면적**은 조정해야 합니다. 데스크톱에서 적당했던 라임 면적이 모바일에서는 화면을 뒤덮습니다.

```css
/* 모바일 — 색면 카드가 세로로 쌓이며 라임 면적이 급증 */
@media (max-width: 767px) {
  .hero {
    /* 대형 라임 색면 → 흰 배경 위주로 전환 */
    background: var(--color-bg-primary);
  }
  .hero__badge { background: var(--color-primary); }  /* 라임은 뱃지로 축소 */

  .card { padding: 20px; border-radius: 20px; }
}

/* 태블릿 — 2열 그리드, 파스텔 카드 교차 배치 */
@media (min-width: 768px) and (max-width: 1023px) {
  .card-grid { grid-template-columns: repeat(2, 1fr); }
}

/* 데스크톱 — 원본의 비대칭 매거진 레이아웃 */
@media (min-width: 1024px) {
  .card-grid { grid-template-columns: 1.4fr 1fr 1fr; }
}
```

**모바일 체크리스트**

- 라임 색면이 첫 화면(above the fold)의 **50%를 넘지 않도록** 합니다.
- 파스텔 카드가 세로로 연속되면 무지개처럼 보입니다. 카드 사이에 흰 여백을 최소 16px 확보하세요.
- 터치 타깃은 최소 44×44px. 라임 원형 버튼이 데스크톱 기준으로 잡히면 모바일에서 작아지기 쉽습니다.

### 3. 접근성 · 환경 대응

```css
/* 고대비 모드 — 파스텔의 미묘한 차이가 무의미해지므로 테두리로 보강 */
@media (prefers-contrast: more) {
  :root {
    --color-text-muted: #FFFFFF;
    --color-text-secondary: #FFFFFF;
  }
  .card { border: 2px solid currentColor; }
}

/* 색상 전환 애니메이션 — 모션 민감 사용자 배려 */
@media (prefers-reduced-motion: reduce) {
  * { transition-duration: 0.01ms !important; }
}

/* 인쇄 — 형광 라임은 인쇄 시 거의 사라지므로 무채색 전환 */
@media print {
  :root {
    --color-bg-primary: #FFFFFF;
    --color-bg-secondary: #FFFFFF;
    --color-primary: #FFFFFF;
    --color-text-primary: #000000;
  }
  .card { border: 1px solid #000000; }
}
```

**색각 이상(Color Vision Deficiency) 고려**

가장 흔한 적록색약(Deuteranopia) 환경에서 이 팔레트를 검증하면:

- ✅ 라임 `#DDFF50` — 밝기 자체가 높아 **명도로 구분**되므로 안전
- ⚠️ 민트 `#7FE0C1` ↔ 블루 `#A8C7FF` — 적록색약에서 **거의 같은 색**으로 보입니다

따라서 카테고리를 색으로만 구분하는 UI는 **반드시 텍스트 레이블이나 아이콘을 함께** 표시해야 합니다. 원본이 각 카드에 "GEO", "SEO", "AEO" 텍스트를 붙여둔 것은 우연이 아니라 올바른 설계입니다.

### 4. 넓은 색역(Wide Gamut) 디스플레이

`#DDFF50`은 sRGB 색역의 경계에 가까운 색입니다. P3 색역을 지원하는 최신 디스플레이에서는 더 선명하게 표현할 수 있습니다.

```css
@supports (color: color(display-p3 1 1 1)) {
  :root {
    --color-primary: color(display-p3 0.855 1 0.345);
  }
}
```

지원하지 않는 환경에서는 자동으로 `#DDFF50`이 유지되므로 안전합니다.

---

## 부록 A. 파생 컬러 산출 근거

원본에 없어 새로 만든 색상의 계산 과정입니다. 모두 **브랜드 색조 Hue 72° / 채도 100%를 고정**한 채 명도만 조절했습니다.

| 토큰 | 값 | 산출 방식 | 검증 |
|---|---|---|---|
| `--color-primary-dark` | `#C3E046` | 원본 이미지의 안티에일리어싱 경계에서 실측 | 라임 대비 명도 -8%p |
| `--color-text-brand` / `--color-link-hover` | `#627A00` | HSL(72°, 100%, 24%) | 흰 배경 대비 **4.88:1** — AA 통과 |
| `--color-text-muted` | `#8A8A8A` | 원본 `#747474`(4.21:1, AA 미달)를 상향 | 다크 배경 대비 **5.70:1** — AA 통과 |

**라임 명도 스케일** (필요 시 확장용)

| 단계 | Hex | 흰 배경 대비 | 다크 배경 대비 | 용도 |
|---|---|---:|---:|---|
| 95 | `#FAFFE5` | 1.02:1 | 19.21:1 | 라임 섹션의 은은한 배경 |
| 88 | `#F3FFC2` | 1.06:1 | 18.65:1 | 호버 배경 |
| 80 | `#EBFF99` | 1.09:1 | 18.09:1 | 칩 배경 (`#ECFF9B`와 근사) |
| 72 | `#E3FF70` | 1.12:1 | 17.62:1 | 다크모드 링크 hover |
| **66** | **`#DDFF50`** | 1.14:1 | **17.34:1** | **기본 Primary** |
| 58 | `#D5FF29` | 1.16:1 | 17.03:1 | 다크모드 Primary 후보 |
| 42 | `#ADD600` | 1.69:1 | 11.62:1 | 경계선·구분자 |
| 34 | `#8CAD00` | 2.59:1 | 7.58:1 | 다크 위 보조 텍스트 |
| **24** | **`#627A00`** | **4.88:1** | 3.99:1 | **흰 배경 위 텍스트/링크** |
| 18 | `#4A5C00` | 7.44:1 | 2.64:1 | 흰 배경 위 제목 |

---

## 부록 B. 적용 체크리스트

포트폴리오에 적용할 때 아래 항목을 순서대로 확인하세요.

**설계 단계**

- [ ] 페이지당 라임 CTA는 1개인가?
- [ ] 라임 면적이 첫 화면의 20%를 넘지 않는가? (모바일 50%)
- [ ] 파스텔이 "장식"이 아니라 "일관된 정보 코드"로 쓰였는가?
- [ ] 60/30/10 비율이 유지되는가?

**구현 단계**

- [ ] 모든 색을 CSS 변수로 참조하는가? (하드코딩된 Hex가 없는가)
- [ ] `:root`에 라이트 팔레트 전체가 정의되어 있는가?
- [ ] `body`에 배경색이 명시되어 있는가?
- [ ] 카드에 `box-shadow: none`과 큰 라운드가 적용되었는가?

**접근성 단계**

- [ ] 흰 배경 위에 `#DDFF50` 텍스트를 쓴 곳이 없는가?
- [ ] 모든 본문 대비비가 4.5:1 이상인가?
- [ ] `:focus-visible` 스타일이 라임/다크 배경 양쪽에서 보이는가?
- [ ] 링크가 색 외에 밑줄로도 구분되는가?
- [ ] 카테고리 구분에 색 외의 레이블이 있는가?

**환경 단계**

- [ ] 다크모드에서 파스텔 채도가 낮춰졌는가?
- [ ] `prefers-reduced-motion` / `prefers-contrast` 대응이 있는가?
- [ ] 인쇄 시 형광색이 무채색으로 전환되는가?

---

*이 문서는 `image.png` 실측 분석을 기반으로 작성되었습니다. 색상 값 변경 시 부록 A의 대비비를 재계산하세요.*
